import React from 'react';

import './styles.css';

const App: React.FC = () => {
    return (
        <div className="filters">
            <header>Filter dimension values</header>
        </div>
    );
};

export default App;
